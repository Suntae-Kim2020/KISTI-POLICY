# 데이터 캐시 (compute.py 생성물, ~30MB)
data_cache.json
data_cache_*.json

# 생성물 폴더 (HTML/PNG/CSV/CDN 캐시)
generated/

# 제외 목록 (런타임 상태)
exclusions.json

# Python
__pycache__/
*.pyc

# macOS
.DS_Store

# 로컬 개발용 사용자 파일 (Secret Manager fallback)
.users_local.json

# 백업 / 생성 산출물
*.bak
KISTI_Policy_*.docx
