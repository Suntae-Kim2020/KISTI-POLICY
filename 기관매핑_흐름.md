# 기관 이명(異名) 매핑 흐름 — 한 장 정리

> **한 줄 요약**: 논문 저자주소(C1)에서 기관명을 뽑아 → **대표명 사전**(고정 엑셀 + 내가 매핑한 json)으로 정규화 → 대표명별로 논문 수를 센다. **엑셀은 안 바꾼다. 내가 매핑한 것만 json에 쌓인다.**

---

## 왜 필요한가
같은 기관이 논문마다 다르게 표기됨 (예: `KISTI`, `Korea Inst Sci & Technol Informat`, `KISTI, Daejeon`).
→ 하나의 **대표명(org_alias)** 으로 통일해야 "기관별 논문건수"가 정확해진다.

---

## 2단(레이어) 구조

```
[레이어 1 · 베이스 = 고정, 매년 안 바뀜]
  기관명데이터.xlsx  ──►  논문마다 "기관명(org) ↔ 대표명(org_alias)" 짝 표
                          (Clarivate 기관 export, 2024 기준)

[레이어 2 · 오버레이 = 매년 내가 쌓는 것]        ◄── 수동 매핑 작업
  ① 새 WoS 논문의 C1(저자주소)에서 기관명 파싱
  ② 베이스에 없는 기관 = "미확인 기관" 자동 탐지 (+추천 대표명 제시)
  ③ 엑셀로 내려받아 → 손으로 대표명 매핑 → 업로드
  ④ institution_mappings.json 에 누적 저장

[분석 시 = 합치기]
  베이스(xlsx) + 오버레이(json)  ──►  "대표명 사전"
  C1의 기관명 → 대표명으로 변환  ──►  대표명별 논문건수 집계
```

---

## 파일별 역할

| 파일 | 역할 | 변경 주기 |
|------|------|-----------|
| `기관명데이터.xlsx` | 베이스 논문×기관 표 (org ↔ 대표명) | **고정** (안 바꿈) |
| `institution_mappings.json` | 내가 수동 매핑한 이명 → 대표명 (누적) | **매년 갱신** |
| WoS `C1` (저자주소) | 새 논문의 기관명 원천 (자동 파싱) | 매년 새로 수집 |
| `wos_institutions.pkl` | 위 3개를 합쳐 만든 최종 논문×기관 연결 | 매년 재생성 |

핵심 함수: `detect_unmatched_institutions()` (build_report.py) — C1 파싱해서 미매칭 기관 탐지.

---

## 매년(내년에도) 할 일

```bash
# 1) 새 연도 WoS 수집 + 미확인 기관 수동 매핑 → institution_mappings.json 갱신
#    (KISTEP 관리 화면에서 엑셀 다운로드 → 매핑 → 업로드)

# 2) 기관 연결 복원 (KISTEP과 같은 로직, 연도만 교체)
python3 build_inst_2025_c1.py --year 2026

# 3) 대시보드 데이터 재생성
python3 compute.py --version 2026 --start-year 2011 --end-year 2026

# 4) 배포 (dev 확인 후 prod)
./deploy.sh dev
./deploy.sh prod
```

---

## 2025 직접논문 복원과의 관계 (2026-07-16)

- **문제**: KISTI_Policy의 `compute.py`는 레이어 2(C1 파싱+오버레이)를 안 하고 엑셀 표만 읽어, 2025 기관연결이 11%만 채워짐 → 직접논문 붕괴(KISTI 5건).
- **해결**: `build_inst_2025_c1.py`가 **KISTEP과 동일한 로직**(C1 파싱 + 대표명 사전 + `institution_mappings.json` 오버레이)으로 2025 연결을 복원.
  → 커버리지 11%→83%, 직접논문 2025 **KISTI 5→47 · KBSI→256 · IBS→360** (평년 수준).
  → 내가 6/27에 손으로 매핑한 결과(GYEONGKUK NATL UNIV, 병원·기업 등)가 여기서 실제 사용됨.
- **caveat**: Clarivate 공식 기관 export가 아니라 C1 자동복원 → established 국내기관은 정확, 미매칭 국내 신설기관 소수(~2%)는 원문명으로 폴백. 유발논문(사사기반)은 영향 없음.

> 출력 위치: `KISTEP/generated/2025/wos_institutions.pkl` (compute.py가 master보다 우선 인식). 기존 KISTEP 파일은 무변경.
